mixin input-login(type)
    .mb-3
        label(class="form-label text-capitalize cursor-pointer" for=type)= type
        input(type="password" id=type name=type class="form-control" placeholder=`Enter your ${type}` required)